app = "go-video-bot"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  DOWNLOAD_DIR = "/app/downloads"
  MAX_FILE_SIZE_MB = "50"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
